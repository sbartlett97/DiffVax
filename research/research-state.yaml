project: DiffVax Extension
question: "How to extend DiffVax image immunization from SD 1.5 (UNet-based, 512x512) to SOTA DiT/hybrid models (FLUX, SD 3.5, gpt-image-edit) and larger resolutions (1088x1088) for a product protecting social media uploads?"

phase: outer_loop_reflection
inner_loop_count: 3
outer_loop_count: 3
last_updated: "2026-04-08T-loop9"

hypotheses:
  H1:
    title: "Multi-model training (SD + FLUX) transfers to unseen DiT architectures"
    status: refuted
    priority: 1
    rationale: >
      DiT-based models (FLUX, SD3.5) share a common VAE latent space structure.
      Perturbations effective at disrupting inpainting in one DiT model likely
      disrupt others because the VAE encoder is the primary attack surface.
      If immunization operates mainly in pixel-space (pre-VAE), it should transfer
      across models that share similar receptive-field properties.
    prediction: "SSIM of edited region < 0.3 on FLUX.1-schnell when model trained on SD1.5+FLUX.2-Klein"
    metric: edit_disruption_rate (edited_SSIM vs clean_SSIM delta)

  H2:
    title: "Patch-based inference enables 1088x1088 immunization without retraining"
    status: confirmed
    priority: 2
    rationale: >
      NestedUNet is fully convolutional — no fixed positional encodings. Processing
      overlapping 512x512 patches with Gaussian-weighted blending (similar to
      stable diffusion upscaling) should produce coherent 1088x1088 immunizations.
      This avoids expensive high-res retraining entirely.
    prediction: "Patch-blended immunization on 1088x1088 achieves >80% of 512-pixel edit disruption rate"
    metric: edit_disruption_rate at 1088x1088 vs 512x512
    result: "CONFIRMED with upside: 50pct_overlap EDR=0.400 vs baseline_512 EDR=0.250 → 1.60x ratio"
    gpu_results:
      baseline_512_edr: 0.250
      no_overlap_edr: 0.300
      overlap_25pct_edr: 0.330
      overlap_50pct_edr: 0.400
      conclusion: "50% overlap (stride=256) is best. 1088px OUTPERFORMS 512px baseline by 1.60x due to perturbation accumulation from overlapping patches. Product default: 1088px + stride=256."
    cpu_preliminary_results:
      no_overlap_seam_ratio: 2.377  # FAIL (threshold 1.2)
      overlap_25pct_seam_ratio: 1.275  # marginal
      overlap_50pct_seam_ratio: 1.046  # PASS — recommended stride=256
      conclusion: "50% overlap (stride=256) required for artifact-free 1088px tiling. This is the product default."

  H3:
    title: "Two-stage curriculum (512 → 1088) outperforms direct high-res training"
    status: untested
    priority: 3
    rationale: >
      The existing configs/train_1024.yml already implements a stage-2 approach.
      Start with a trained 512 checkpoint and fine-tune at 1088 with reduced LR.
      The fine-tuning needs to handle the non-square 1088 resolution and
      resolution-aware loss scaling (currently hardcoded /512).
    prediction: "Stage-2 fine-tuning converges in <50% the iterations of training from scratch at 1088"
    metric: convergence_iterations (to hit threshold edit_loss)

  H4:
    title: "VAE feature-space immunization generalizes better than pixel-space to DiT models"
    status: untested
    priority: 4
    rationale: >
      Instead of/in addition to matching denoised output to zeros, adding a loss
      term that maximizes VAE-encoded latent distance (||VAE(x+δ) - VAE(x)||)
      could make perturbations that survive the encoder bottleneck and thus
      disrupt DiT models that use different UNets but the same/similar VAE.
    prediction: "VAE-feature loss term improves cross-model transfer by >15% edit disruption rate"
    metric: cross_model_edit_disruption_rate

  H5:
    title: "Frequency-domain perturbations (DCT/wavelet) are more perceptually efficient at high resolution"
    status: deprioritized
    priority: 6
    note: "REVISED: High-freq DCT perturbations are MORE vulnerable to JPEG (DCT-Shield, ICCV 2025). H5 needs redesign around JPEG-quantization-aware constraints. Superseded by H7 for social media use case."
    rationale: >
      At 1088x1088, a flat epsilon-bound in pixel space may be visually detectable
      at low spatial frequencies. Imposing perturbations mainly in mid/high
      frequency bands (DCT masking) keeps them imperceptible while maximally
      disrupting the VAE encoder's low-frequency reconstruction.
    prediction: "Frequency-constrained training improves SSIM of immunized image by >2dB while maintaining edit disruption"
    metric: [immunized_SSIM, edit_disruption_rate]

  H7:
    title: "JPEG-augmented training produces immunizations that survive social media upload compression"
    status: refuted
    priority: 2
    rationale: >
      Instagram (~q=75) and Twitter (~q=70) apply JPEG compression to all uploads.
      Standard DiffVax (Lp-bounded pixel-space) is eliminated by this compression.
      Training with JPEG augmentation (STE gradient, q=70-85) forces perturbation
      energy into DCT bands that survive typical social media quality settings.
    prediction: "H7 checkpoint maintains EDR ≥ 0.7 after q=75 JPEG; H1a baseline drops to ≤ 0.5"
    metric: post_jpeg_edit_disruption_rate at q=75 and q=70
    config: configs/train_multimodel_h7.yml
    reference: "DCT-Shield (ICCV 2025, arXiv:2504.17894)"

  H1b:
    title: "Gradient-normalized SD+FLUX training recovers multi-model benefit"
    status: protocol_committed
    priority: 3
    rationale: >
      H1a failed because FLUX gradients (Loss1≈1.0) dominate SD15 gradients (Loss1≈0.08)
      by ~12x, weakening the total perturbation. Per-model loss normalization (divide each
      model's Loss1 by its expected magnitude) equalizes gradient contributions without
      changing the routing probabilities. Implemented via MultiAttack.current_loss_scale
      property and config: sd_loss_scale=0.08, flux_loss_scale=1.0.
    prediction: "H1b achieves FLUX EDR ≥ sd15_only (0.200) while maintaining SD15 EDR ≥ 0.280"
    config: configs/train_multimodel_h1b.yml
    metric: [flux_edr, sd15_edr, sd35_edr]

  H1c:
    title: "FLUX+SD3.5 (DiT-only, shared VAE family) curriculum outperforms sd15_only on DiT models"
    status: new
    priority: 4
    rationale: >
      User insight: FLUX and SD3.5 share 16-channel VAE architecture. Training on two
      architectures from the same VAE family produces more coherent gradient signals than
      FLUX+SD15 (different VAE dimensions: 4ch vs 16ch). If FLUX and SD3.5 produce
      similar Loss1 magnitudes, the curriculum is naturally balanced. Trade-off: SD15
      EDR may drop (not trained), but SD15 is becoming legacy.
    prediction: "H1c achieves FLUX EDR ≥ 0.250 and SD35 EDR ≥ 0.180 (vs sd15_only 0.200/0.140)"
    config: configs/train_multimodel_h1c.yml
    note: "Requires measuring SD3.5 Loss1 magnitude in first 100 steps; adjust sd3_loss_scale if needed"
    metric: [flux_edr, sd15_edr, sd35_edr]

  H8:
    title: "Single DiT training (FLUX-only) produces stronger DiT-family perturbations than SD1.5-only"
    status: new
    priority: 2
    rationale: >
      H1 showed: (a) sd15_only already transfers to FLUX/SD35 via shared convolutional VAE
      bottleneck; (b) multi-model training fails due to gradient competition. The natural
      follow-on: does it matter which single model we train on? sd15_only uses a 4-channel
      VAE; FLUX uses a 16-channel VAE. Training exclusively on FLUX (single-objective,
      preserving "less is more") should produce perturbations that are:
        - Stronger against FLUX (single-objective optimisation directly on FLUX)
        - Stronger against SD3.5 (same 16-ch VAE family → better spatial representation)
        - Possibly weaker against SD1.5 (different 4-ch VAE dimension)
      If flux_only achieves FLUX EDR >> sd15_only (0.200) while maintaining any SD15 transfer,
      it suggests the VAE family dimension matters and modern DiT training is strictly superior.
      This also tests whether the JPEG paradox is enhanced when the base perturbation is
      explicitly tuned to FLUX's patch tokenizer architecture.
    prediction: >
      flux_only achieves FLUX EDR ≥ 0.300 (vs sd15_only 0.200), SD35 EDR ≥ 0.200
      (vs sd15_only 0.140), SD15 EDR possibly 0.100-0.200 (weaker than sd15_only 0.300),
      PSNR ~32-33 dB, JPEG paradox enhanced (q=75 FLUX EDR ≥ 0.450)
    config: configs/train_flux_only.yml
    metric: [flux_edr, sd15_edr, sd35_edr, psnr_immunized, jpeg_flux_edr_q75]
    note: >
      Training-duration fairness: H8 short (max_steps=16000) matches H1a compute.
      H8 long (max_steps=112000) matches sd15_only effective compute.
      Run short first; if FLUX EDR > 0.250, run long for matched comparison.
    user_motivation: >
      User insight 2026-04-09: "most modern DiT-based models have a 16-channel VAE latent
      space — replace original SD1.5 training with a single DiT inpainting model."
      This directly tests VAE-family hypothesis with maximum perturbation strength.

  H6:
    title: "Multi-model immunization resists FLUX-based purification attacks"
    status: refuted
    priority: 2
    rationale: >
      "Purify Once, Edit Freely" (arXiv:2603.13028) shows FLUX.1-fill-dev can purify
      SD1.5 immunizations by +3-6dB PSNR. If DiffVax is trained against FLUX,
      the purifier can no longer recover editability because the immunization
      specifically corrupts FLUX's latent representation. This makes multi-model
      training a product safety requirement, not just a research nicety.
    prediction: "FLUX-based EditorClean purifier fails on DiffVax-FLUX immunized images (SSIM delta < 0.05)"
    metric: edit_disruption_rate after purification attempt

current_focus: >
  NEW DIRECTION (2026-04-09): H8 — single DiT training (flux_only).
  User insight: replace SD1.5 training with FLUX.1-schnell as the single attack model.
  Config written: configs/train_flux_only.yml. Ready to run on A100.
  Prediction: FLUX EDR ≥ 0.300, SD35 EDR ≥ 0.200, possible JPEG paradox enhancement.

  PAPER STATUS: LaTeX draft complete (paper/latex/main.tex, 570 lines, ICLR 2026).
  H8 results, if obtained, would go in Table 3 (cross-architecture transfer) and
  would strengthen the "VAE family matters" narrative.
  Training-duration confound noted in §5.2.

  PREVIOUS: H7 RESULTS IN AND ANALYZED (2026-04-09). ALL GPU EXPERIMENTS COMPLETE.
  H7 REFUTED: STE JPEG aug training further weakens perturbation (PSNR=35.65 vs H1a 34.81).
  No JPEG paradox for H7. The paradox is emergent from sd15_only's strong perturbation.

  COMPLETE RESULT PICTURE:
  - H2: CONFIRMED 1.60× (sd15_only + 1088px patch, stride=256)
  - H1: REFUTED (multi-model weakens perturbation by 2.1 dB)
  - H6: REFUTED (H1a weaker; sd15_only already survives light purification)
  - H7: REFUTED (JPEG aug further weakens; no paradox amplification)
  - SURPRISE: sd15_only transfers to FLUX (0.200) and SD3.5 (0.140) via shared VAE
  - SURPRISE: JPEG q=75 increases sd15_only FLUX EDR 0.200→0.300 (+50%)
  - SURPRISE: Light FLUX purification (s=0.3) fails on sd15_only (net +0.200)

  BEST SYSTEM: sd15_only + 1088px patches
  FUTURE WORK: H1b (gradient normalization), H1c (DiT curriculum)

  PAPER STATUS: COMPLETE LATEX DRAFT WRITTEN (2026-04-09).
  paper/latex/main.tex: 527 lines, ICLR 2026 format, 9 sections, 5 tables, 5 equations.
  paper/latex/references.bib: 20 entries (verified + placeholders marked [PLACEHOLDER]).
  paper/diffvaxpp_submission.zip: Overleaf-ready package.
  NEEDS: pdflatex compile (no LaTeX installed on machine), citation verification for [PLACEHOLDER] entries.
  PAPER NARRATIVE: "less is more" — H2 confirmed (1.60×) + 3 surprising negative results revealing DiffVax already robust.

  REVISED H1/H6 RESULTS ANALYZED (2026-04-08 loop-9). Both hypotheses REFUTED.

  H1 RESULT: sd15_only (PSNR=32.71) outperforms H1a (PSNR=34.81) on all models:
    sd15_only: SD1.5=0.300, FLUX=0.200, SD3.5=0.140
    H1a: SD1.5=0.290, FLUX=0.140, SD3.5=0.060
  Multi-model training weakens the perturbation. H1a is strictly inferior.

  H6 RESULT: sd15_only net survival (s=0.3) = +0.200, H1a = +0.133. sd15_only is better.

  KEY DISCOVERIES (publishable):
  - sd15_only already transfers to FLUX (0.200) and SD3.5 (0.140) via shared VAE
  - JPEG paradox: FLUX EDR increases 50% at q=75 (0.200 → 0.300) for sd15_only
  - Light FLUX purification (s=0.3) fails to remove sd15_only immunization (net +0.200)
  - EditorClean paper's assumption of zero purification cost is incorrect in practice

  PAPER STORY PIVOT: From "three confirmed contributions" to "surprising negative results
  that reveal unexpected DiffVax robustness" + H2 (confirmed 1.60×) + JPEG paradox + H7.

  NEXT: H7 results pending. Update paper after H7 results arrive.
  Paper [X] placeholders remaining: H7 table, ablation tables, combined system final row.

literature_key_papers:
  - "arXiv:2603.13028 — Purify Once Edit Freely: FLUX defeats SD1.5 immunization (CRITICAL)"
  - "arXiv:2602.14679 — Universal Immunization via Semantic Injection (cross-model transfer works)"
  - "arXiv:2503.10081 — AdvPaint: FLUX-Fill specific attention disruption"
  - "arXiv:2507.09595 — Demystifying FLUX Architecture"
  - "arXiv:2507.03953 — Benchmark of 8 protection methods (evaluation methodology)"

known_constraints:
  - "No local GPU. All experiments require cloud/cluster submission (Modal, Lambda Labs)."
  - "gpt-image-edit is a black-box API — cannot do differentiable attacks through it. Transfer-only."
  - "FLUX attention hook manager captures attn maps only when model returns them — may need to patch diffusers to get maps in all cases."
  - "SD 3.5 may require bfloat16 (not float16) on some GPU configs."

resolved_constraints:
  - "FIXED 2026-04-08: train.py missing jpeg_augment_prob/jpeg_quality_range/checkpoint_every forwarding to DiffVaxImmunization — H7 JPEG augmentation was silently disabled. Fixed."
  - "FIXED 2026-04-08: H6 purification mask was all-zeros (no-op). FluxAttack denoising step resets latents to original at every step when mask=0. Fixed to all-ones (full reconstruction)."
  - "VERIFIED 2026-04-08: H2 CSV numbers confirmed from raw data: baseline_512=0.250, no_overlap=0.300, 25pct=0.330, 50pct=0.400, ratio=1.60x — matches paper exactly."
  - "SEARCHED 2026-04-08: 2025-2026 literature sweep — no new immunization competitors found that beat DiffVax++ on any deployment dimension. Competitive positioning holds."
  - "FIXED 2026-04-06: Loss /512 → /h (resolution-agnostic)"
  - "FIXED 2026-04-06: FLUX attack wrapper implemented (attack_flux.py)"
  - "FIXED 2026-04-06: SD 3.5 attack wrapper implemented (attack_sd3.py)"
  - "FIXED 2026-04-06: Multi-model routing implemented (attack_multi.py)"
  - "FIXED 2026-04-06: Patch-based 1088 inference implemented (patch_immunize.py)"
  - "FIXED 2026-04-07: VAE feature loss implemented in DiffVaxImmunization"
  - "FIXED 2026-04-07: Attention hook manager implemented (losses.py)"
  - "FIXED 2026-04-07: generate_masks.py --size parameter added for 1088px data generation"
  - "VERIFIED 2026-04-07: attack_flux.py API confirmed correct against diffusers docs (img_ids, txt_ids, timestep/1000, guidance tensor, scaling/shift_factor from config)"
  - "FIXED 2026-04-08: attack_sd3.py CPU offload removed, gradient checkpointing added"
  - "VALIDATED 2026-04-08: H7 STE JPEG gradient flow correct (identity backward, q=70/75 forward match)"
  - "FIXED 2026-04-08: eval_transfer.py model.eval() bug + per-model inference steps (FLUX.1-schnell uses 4 steps)"
  - "FIXED 2026-04-08 (CRITICAL): eval_transfer.py and eval_purification_robustness.py model.eval() → model.train(). BN running stats from batch_size=1 training collapse activations 78x in eval mode. All H1/H6 results from first run invalid, must re-run."
  - "FIXED 2026-04-08: H6 eval added clean-image purification control (purification_control_disrupted column) to detect purifier-damage confound at high strengths."
  - "VERIFIED 2026-04-08 (CPU): patch_coverage_analysis.py — center_cov=4 at stride=256 confirmed. EDR vs center-coverage correlation r=0.9563. Figure generated: research/to_human/figures/patch_coverage_density.png"

decisions:
  - "2026-04-06: Bootstrap complete. All wrappers implemented."
  - "2026-04-07: H4 VAE feature loss added to training loop (config: train_multimodel_h4.yml)"
  - "2026-04-07: Run order: H2 (free) → H1a → H6 → H3 (if H2 fails) → H4 → H7"
  - "2026-04-07: H2 CPU prelim — stride must be 256 (50% overlap) to avoid seam artifacts at 1088px. Updated patch_immunize.py default."
  - "2026-04-07: H7 added — JPEG-robust training is critical for social media product (Instagram/Twitter apply q=70-75)"
  - "2026-04-07: FLUX model changed from FLUX.2-Klein-4B → FLUX.1-schnell. Klein is T5-only (no CLIP), incompatible with FluxInpaintPipeline."
  - "2026-04-07: eval_transfer.py extended with --jpeg-qualities flag for H7 evaluation"
  - "2026-04-07: H2 GPU results — CONFIRMED at 1.60x. 50pct_overlap EDR=0.400 vs baseline_512 EDR=0.250. Product default: 1088px + stride=256."
